/**
 * 子应用 → 宿主的通信桥接层
 *
 * 负责向主应用发出指令（导航、登出、弹窗等）以及订阅主应用事件。
 * 所有跨边界的主动调用统一收口于此，业务层不直接操作 qiankun props。
 */

/**
 * 唤起宿主的退出登录确认弹窗
 *
 * TODO: 等主应用在 props 中注入对应回调后实现
 */

export const showLogoutModal = () => {}
