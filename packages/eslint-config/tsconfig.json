{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // ESLint 配置包特殊选项：直接使用 .ts 文件，不编译输出
    "allowImportingTsExtensions": true,
    "noEmit": true
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules"]
}
